{
  "name": "booking_management_system",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "install-deps": "npm install",
    "build": "npm run build --prefix backend && npm run build --prefix frontend",
    "migrate": "npm run migrate --prefix backend",
    "start": "concurrently \"npm run dev --prefix backend\" \"npm run start --prefix frontend\""
  },
  "keywords": [],
  "author": "AgileDataLabs",
  "devDependencies": {
    "concurrently": "^7.0.0"
  }
}
